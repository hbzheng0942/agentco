# retriever — 检索蒸馏。只读 search.py 产出的 raw,不联网。
# turn 上限 5:codex 0.142.5 无原生 max_turns 字段(已 VERIFY),经 dispatcher ttl_sec 兜底 + instructions 约束。
# model:ds-chat(非推理)。ds-reasoner=deepseek-v4-pro 在 Codex 工具循环里因 reasoning_content 回传要求恒 400,
# 检索蒸馏不需要深推理,换 ds-chat 确定能跑通工具循环(auditor 仍用 ds-reasoner,靠 litellm_hooks 补 reasoning_content)。
model = "ds-chat"
model_provider = "litellm"
sandbox_mode = "read-only"
model_instructions_file = "/opt/agentco/config/profile-instructions/retriever.md"
